- [ ] 前端已接入 Vue Router、Element Plus、Axios（可选 Pinia），页面可正常渲染
- [ ] Vite 代理 `/api` 生效，开发模式可直接调用后端 API
- [ ] 登录成功后保存 token，并能访问 `GET /api/users/me`
- [ ] 未登录访问业务路由会跳转到登录页（路由守卫生效）
- [ ] 知识库：可创建、列表展示、删除（与 API.md 一致）
- [ ] 文档：可上传（keep/replace）、列表展示、删除（与 API.md 一致）
- [ ] 问答：`POST /api/rag/chat` 可返回并展示非空自然语言 `answer`
- [ ] 关键异常有可理解提示（400/401/404/500），不暴露敏感信息
- [ ] `npm run build` 成功

